import { IColor } from 'enta';

export default interface IColorRange {
    opacity100: IColor;
    opacity80: IColor;
    opacity60: IColor;
    opacity40: IColor;
    opacity20: IColor;
    opacity10: IColor;
}
